# Load environment variables from .env if present (direnv stdlib)
dotenv_if_exists .env

# Enter the Nix dev shell defined by the root flake (requires nix-direnv)
use flake

# Re-enter when these change
watch_file flake.nix
watch_file flake.lock

